/** Host loader entry for the agent-notes Settings section exported from `./client`. */

/** Host plugin body — no host-side behavior for the Agent Notes settings section. */
export function apply(): void {}
